spring:
  profiles:
    active: test
  datasource:
    embedded-database-connection: h2
    driver-class-name: org.h2.Driver
  jpa:
    show-sql: true

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG